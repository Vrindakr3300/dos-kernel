# `spikes/` — preserved design screenplays

Scratch design artifacts kept as provenance for features that later shipped —
not active code. Nothing imports from here; nothing here runs in CI.

| File | The shipped feature it designed |
|---|---|
| `state_home_design.wf.js` | the `.dos/` state-home model (where the kernel keeps per-workspace runtime state) |
