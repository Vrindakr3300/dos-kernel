"""FleetHorizon — the open-loop vs closed-loop fleet A/B benchmark for DOS.

See README.md. Consumes the DOS kernel from outside (the `examples/` boundary);
nothing under `src/dos/` imports this.
"""
